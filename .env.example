# Lavender Ledger Environment Variables
# Copy this file to .env and customize for Docker deployment

# Terminal password for accessing the web-based terminal
# This is used when running the dashboard in Docker
TERMINAL_PASSWORD=lavender

# Optional: Override data directory path
# DATA_DIRECTORY=/data

# Optional: Override database path
# DATABASE_PATH=/data/finance.db

# Note: Claude Code state is persisted via volume mount
# The ~/.claude directory is automatically mounted to preserve:
# - Authentication credentials
# - User settings and preferences
# - Project history and context
# - Task/todo lists
